import styles from "./stylesDesktop.css";

import MenuDesktop from "../MenuDesktop";

const HiddenMenuDesktop = (props) => {

    return (
        <MenuDesktop {...props}/>
    );
};

export default HiddenMenuDesktop;